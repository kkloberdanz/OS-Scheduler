#ifndef HELPERS_HPP
#define HELPERS_HPP

#include <string>
#include <vector>

bool is_num(std::string);

int as_int(std::string);

std::vector<std::string> split(std::string s, char);

#endif // HELPERS_HPP
